Notes for OpenSSL 0.9.8g (Antares r24681)
=======================================

configure
---------

  ./config --prefix=/boot/home/config zlib shared no-sse2

* zlib instead of zlib-dynamic: dlopen() in Antares needs fixing.
* no-sse2: Antares SSE2 support is broken/non-existent.

make/install
------------

  make
  make install

