SubDir ANTARES_TOP src tests kits interface bshelf ;

SetSubDirSupportedPlatformsBeOSCompatible ;

UsePrivateHeaders interface ;

if $(TARGET_PLATFORM) != antares {
	UseHeaders [ FDirName $(ANTARES_TOP) headers os interface ] : true ;
	UseHeaders [ FDirName $(ANTARES_TOP) headers os ] : true ;
	SubDirC++Flags -D_SetShelf=set_shelf ;
}

SimpleTest ShelfTest :
        ShelfTest.cpp
        : be $(TARGET_LIBSUPC++)
        ;

SEARCH on [ FGristFiles
                Shelf.cpp
        ] = [ FDirName $(ANTARES_TOP) src kits interface ] ;
